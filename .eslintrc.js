module.exports = {
    // Các cài đặt khác của ESLint
    rules: {
      'eslint-disable-next-line': 'off', // Tắt cảnh báo về việc sử dụng eslint-disable-next-line
      // Các quy tắc khác nếu cần
    },
  };
  